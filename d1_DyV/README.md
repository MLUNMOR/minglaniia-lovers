Para poder solucionar este problema hemos usado la técnica DyV, para poder calcular antes la posición a la que se va a mover el dial y si va a caer en 0. Para resolver el problema calculamos el mov, y observamos si cae en 0 para incrementar el valor de la contraseña.

Este problema no ha representado una gran dificultad, aunque es difícil emplear los métodos requeridos ya que se puede resolver fácilmente con fuerza bruta, y al ser problemas simples, la diferencia es prácticamente insignificante.
